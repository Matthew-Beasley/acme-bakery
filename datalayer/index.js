const {
  createChefs,
  readChefs
} = require('./chefs');

const {
  createRecipes,
  readRecipes
} = require('./recipes');


module.exports = {
  createChefs,
  readChefs,

  createRecipes,
  readRecipes
};